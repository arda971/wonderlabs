extends layout

- var usr= user
block content
  h1= title
  p Welcome to - #{title}

  if usr
    p  #{user}
    br
    a.btn.btn-danger(href='/signin') Logout;
  else
    br
    a.btn.btn-danger(href='/auth/login') Login

  br
  a.btn.btn-danger(href='/users') Go To Protected Endpoint
